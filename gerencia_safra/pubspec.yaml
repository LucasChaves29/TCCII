name: gerencia_safra
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  sqflite: ^2.1.0
  path: ^1.9.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.10.0
  cupertino_icons: ^1.0.8
  tflite_flutter: ^0.11.0
  http: ^1.2.2
  fl_chart: ^0.69.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

assets:
  - assets/modelo_recomendacao_solo.tflite
